function Mostrar()
{

    //tomo la edad invocando la variable para determinar el dato referente a mi condicion

    var edad;

    edad = document.getElementById("edad").value

    // If se utiliza para establecer una condicion a cumplir y dentro de ello se coloca las indicaciones 

    if ( edad ==15)

    {
        alert("Niña Bonita");
    }

    //Entre llaves podre poner el alert con el mensaje a mostrar segun se cumpla la condicion de if
}

//FIN DE LA FUNCIÓN